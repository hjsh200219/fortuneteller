# Open Questions

## manseryeok-bug-fixes - 2026-03-19

### 해결됨

- [x] 기준일 결정 (갑술일 vs 병자일) -- JDN(율리우스 일수) 기반 천문학적 검증으로 **병자일(stemIndex=2, branchIndex=0)이 정답**임을 확인. 11개 날짜 교차 검증 모두 통과. 커밋 8000c0c의 갑술일 수정은 잘못된 수정.

### 미해결

- [ ] 야자시 기본 설정을 "다음 날 자시"로 할지 "당일 자시"로 할지 -- 사용자층이 어느 학파를 주로 따르는지에 따라 기본값이 달라져야 함. 자평명리(다수파)는 다음 날, 일부 현대 명리가는 당일 사용. 현재 계획에서는 "다음 날"을 기본값으로 설정.
- [ ] getCurrentSolarTerm 근사 함수를 제거할지 fallback으로 유지할지 -- 정밀 함수로 교체 시 근사 함수가 불필요하지만, 절기 테이블 범위 밖(2200년 이후)에서의 fallback 역할 가능. 현재 계획에서는 함수 정의는 유지하되 호출 0건으로 만듦.
- [ ] 교차 검증에 사용할 공신력 있는 만세력 출처 -- 한국천문연구원 데이터 또는 특정 만세력 사이트(예: 만세력닷컴) 중 어느 것을 기준으로 삼을지 결정 필요. 현재 계획에서는 JDN 기반 수학적 검증 + 기존 manselyeok_table.ts 데이터를 기준으로 사용.
- [ ] helpers.ts의 getDayPillar와 saju.ts의 calculateDayPillar 중복 문제 -- 동일 로직이 두 곳에 존재. 장기적으로 하나로 통합하는 것이 바람직하나, 이번 수정에서는 양쪽 모두 병자일 + Date.UTC로 일관성만 확보하고 통합은 후속 과제로 분리.
